spring.application.name=boat-manager

# Default profile - use 'h2' for in-memory or 'postgresql' for external database
# Override at startup: --spring.profiles.active=postgresql or -Dspring.profiles.active=postgresql
spring.profiles.active=h2

# JPA common settings
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
